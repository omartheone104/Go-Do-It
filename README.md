# Go Do It

A free, privacy-first desktop to-do list + calendar app built with C# and Avalonia UI.

## Quick Start

1. **Prerequisites**
   - .NET 8.0 SDK (download from https://dotnet.microsoft.com/download)
   - Git

2. **Clone and build**
   ```bash
   git clone https://github.com/omartheone104/Go-Do-It.git
   cd Go-Do-It
   dotnet restore
   dotnet build
   ```

3. **Run the app**
    ```bash
    dotnet run --project GoDoIt/GoDoIt.csproj
    ```

4. **Run unit tests**
    ```bash
    dotnet test
    ```